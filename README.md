# AutoDesk-DSA
C++ dealership management system using a linked list for inventory, stack-based recycle bin for soft deletes, and dual Binary Search Trees for O(log n) search by ID and price — with role-based login and binary file persistence.
